c     wap to add two numbers

      program addition
      integer a,b,c
      write(*,*) 'enter any two numbers'
      read(*,*) a,b
      c=a+b
      write(*,*)'the sum is',c
      read(*,*)
      end
