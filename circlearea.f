c       to find the area of circle
      program finding the area of circle
      
      real area,r
      write(*,*) 'Enter the radius of circle'
      read (*,*) r
      area= 3.1415*r*r
      write(*,*) 'the area of circle is',area
      read(*,*)
      end
